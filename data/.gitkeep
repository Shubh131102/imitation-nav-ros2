
# Data Directory

This folder contains training and evaluation datasets.

## Structure
```
data/
├── expert_demos/        Expert trajectories from Nav2
├── dagger_iterations/   Data collected during DAgger training
├── evaluation/          Test scenario results
└── processed/           Preprocessed datasets ready for training
```

## Data Format

**Expert demonstrations (CSV):**
```
timestamp, lidar_0, lidar_1, ..., lidar_179, goal_x, goal_y, linear_vel, angular_vel
```

**Evaluation logs (JSON):**
```json
{
  "episode_id": 1,
  "environment": "complex",
  "success": true,
  "uncertainty_mean": 0.24,
  "collisions": 0
}
```

## Notes

Large data files are tracked with Git LFS. Raw bags are not committed.
To download datasets: [Add link if hosted externally]
